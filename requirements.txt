requests>=2.31          # Ticketmaster API, MusicBrainz, and the Supabase REST endpoint
python-dotenv>=1.0      # loads keys from .env so they never touch the code
tqdm>=4.66              # progress bars on long runs
pandas>=2.2             # dataframes for analysis
pyarrow>=15.0           # Parquet export
matplotlib>=3.8         # figures
numpy>=1.26
